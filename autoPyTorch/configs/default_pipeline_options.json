{
    "device": "cpu",
    "budget_type": "epochs",
    "epochs": 50,
    "runtime": 3600,
    "torch_num_threads": 1,
    "early_stopping": 20,
    "use_tensorboard_logger": "False",
    "metrics_during_training": "True"
}
